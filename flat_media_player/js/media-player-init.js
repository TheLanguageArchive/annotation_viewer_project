/**
 * Placeholder for media-player-init.js to avoid 404 errors.
 * The actual initialization is handled by media-player-drupal.js.
 */
(function ($, Drupal, drupalSettings) {
    // Initialization logic is in media-player-drupal.js
})(jQuery, Drupal, drupalSettings);
